# =============================================================================
# OpenClaw Workflow Environment Variables
# =============================================================================
# This file documents ALL environment values required by the GitHub Actions
# workflows (create-new-openclaw, update-openclaw, delete-moltworker).
#
# HOW TO USE:
#   1. Copy this file, fill in real values
#   2. Upload each variable as a GitHub Repository Secret
#      (Settings > Secrets and variables > Actions > New repository secret)
#   3. The workflow YAML files reference these as ${{ secrets.<NAME> }}
# =============================================================================

# -----------------------------------------------------------------------------
# [REQUIRED] Cloudflare Authentication
# -----------------------------------------------------------------------------
# API token for wrangler CLI. Must have permissions:
#   - Workers Scripts: Edit
#   - Workers R2 Storage: Edit
#   - Account Settings: Read
# Create at: https://dash.cloudflare.com/profile/api-tokens
# GitHub Secret name: CLOUDFLARE_API_TOKEN
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token

# Cloudflare Account ID
# Found at: https://dash.cloudflare.com > any domain > Overview (right sidebar)
# GitHub Secret name: CF_ACCOUNT_ID
CF_ACCOUNT_ID=your-cloudflare-account-id

# -----------------------------------------------------------------------------
# [REQUIRED] Cloudflare AI Gateway
# -----------------------------------------------------------------------------
# The API key sent to the AI Gateway. This is the underlying provider key
# (e.g. Anthropic API key, OpenAI API key, or CF Workers AI API token)
# routed through the gateway. The actual provider is determined by the
# "model" input at workflow dispatch time:
#   - workers-ai/<model-id>   → Cloudflare Workers AI
#   - openai/<model-id>       → OpenAI
#   - bedrock/<model-id>      → AWS Bedrock
# GitHub Secret name: CLOUDFLARE_AI_GATEWAY_API_KEY
CLOUDFLARE_AI_GATEWAY_API_KEY=your-provider-api-key

# AI Gateway Account ID (same as CF_ACCOUNT_ID in most cases)
# GitHub Secret name: CF_AI_GATEWAY_ACCOUNT_ID
CF_AI_GATEWAY_ACCOUNT_ID=your-cloudflare-account-id

# AI Gateway ID — the specific gateway created in the Cloudflare dashboard
# Create at: https://dash.cloudflare.com > AI > AI Gateway
# GitHub Secret name: CF_AI_GATEWAY_GATEWAY_ID
CF_AI_GATEWAY_GATEWAY_ID=your-gateway-id

# -----------------------------------------------------------------------------
# [REQUIRED] R2 Persistent Storage Credentials
# -----------------------------------------------------------------------------
# R2 API token for rclone-based backup/restore inside the container.
# Create at: https://dash.cloudflare.com > R2 > Manage R2 API Tokens
# The token needs Object Read & Write on the relevant buckets.
# GitHub Secret name: R2_ACCESS_KEY_ID
R2_ACCESS_KEY_ID=your-r2-access-key-id

# GitHub Secret name: R2_SECRET_ACCESS_KEY
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key

# -----------------------------------------------------------------------------
# [OPTIONAL] Cloudflare Access (Admin UI protection)
# -----------------------------------------------------------------------------
# Team domain for Cloudflare Access (e.g. myteam.cloudflareaccess.com)
# Required if you want the /_admin UI protected by Zero Trust.
# GitHub Secret name: CF_ACCESS_TEAM_DOMAIN
CF_ACCESS_TEAM_DOMAIN=myteam.cloudflareaccess.com

# Application Audience (AUD) tag from the Access Application
# GitHub Secret name: CF_ACCESS_AUD
CF_ACCESS_AUD=your-access-application-audience-tag

# =============================================================================
# NOTES
# =============================================================================
# - MOLTBOT_GATEWAY_TOKEN is auto-generated per user by the create workflow.
#   No need to set it as a repo secret.
# - TELEGRAM_BOT_TOKEN is provided as a workflow input at dispatch time.
#   No need to set it as a repo secret.
# - CF_AI_GATEWAY_MODEL is derived from the "model" workflow input.
#   No need to set it as a repo secret.
# - R2_BUCKET_NAME is auto-set to "openclaw-<username>-data" by the workflow.
#   No need to set it as a repo secret.
# - Each deployed moltworker is named "openclaw-<username>" and gets its own
#   R2 bucket "openclaw-<username>-data" for data isolation.
